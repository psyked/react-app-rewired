{
  "private": true,
  "scripts": {
    "postinstall": "lerna bootstrap",
    "test": "lerna run test",
    "publish": "lerna publish && lerna-npm-readme-hack"
  },
  "devDependencies": {
    "lerna": "2.0.0",
    "react-scripts": "2.1.2"
  }
}
