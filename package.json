{
  "private": true,
  "scripts": {
    "postinstall": "lerna bootstrap",
    "test": "lerna exec yarn test --stream",
    "publish": "lerna publish && lerna-npm-readme-hack"
  },
  "devDependencies": {
    "lerna": "2.0.0",
    "react-scripts": "2.1.2"
  }
}
